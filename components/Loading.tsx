/**
 * MODULE:  components/Loading
 * 
 * SUMMARY:
 *   Displays loading screen.
 *
 * DEPENDENCIES:
 *   none
 * 
 * USED BY:
 *   Various pages that need to fetch data from database
 * 
 * NOTE: I intend to improve this in the future.
 */

export default function Loading() {
  return (
    <div>Loading...</div>
  );
}